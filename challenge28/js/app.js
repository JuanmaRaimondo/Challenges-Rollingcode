//Realizar una calculadora simple que incluya suma, resta, multiplicación y división.

let operation = parseInt(prompt('Ingrese la operacion que desee realizar. para suma 1, para resta 2, para multiplicacion 3 y division 4.'));
let firstNumb, secondNumb, result;

if (operation = isNaN || operation <= 0 || operation >= 5) {
    alert('Porfavor ingrese un numero del 1 al 4');
} else {
    switch (operation) {
        case value:
            console.log(`la suma entre ${firstNumb} y ${secondNumb} es igual a ${firstNumb + secondNumb}`)
            break;

        case value:
            console.log(`la resta entre ${firstNumb} y ${secondNumb} es igual a ${firstNumb - secondNumb}`);
            break;

        case value:
            console.log(`la multiplicacion entre ${firstNumb} y ${secondNumb} es igual a ${firstNumb * secondNumb}`);
            break;

        case value:
            console.log(`la division entre ${firstNumb} y ${secondNumb} es igual a ${firstNumb / secondNumb}`);
            break;

        default: operation = isNaN
            break;
    }

};






